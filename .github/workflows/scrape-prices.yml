name: Actualizar precios Amazon

# Para Fnac y ECI usamos el feed oficial de Awin (import-awin-feed.yml).
# PcComponentes está pausado de momento.
# Aquí solo scrapeamos Amazon, que no tiene feed Awin disponible.

on:
  schedule:
    # Cada 6 horas: 00:00, 06:00, 12:00, 18:00 UTC
    - cron: "0 */6 * * *"
  workflow_dispatch: # también ejecutable manualmente desde GitHub

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Instalar dependencias
        run: npm ci

      - name: Generar cliente Prisma
        run: npx prisma generate

      - name: Ejecutar scraper (solo Amazon)
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: npx tsx scripts/scrape-prices.ts amazon
